version: '3.9'

services:
  db:
    image: postgres:16.2
    container_name: db_cloud
    restart: always
    volumes:
      - ~/db_cloud:/var/lib/postgresql/data
    ports:
      - "5454:5432"
    environment:
      POSTGRES_DB: cloud
      POSTGRES_USER: root
      POSTGRES_PASSWORD: xLx210ET5EMkD
